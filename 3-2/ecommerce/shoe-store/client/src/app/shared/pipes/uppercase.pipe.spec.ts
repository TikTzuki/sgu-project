/* tslint:disable:no-unused-variable */

import {UppercasePipe} from './uppercase.pipe';

describe('Pipe: Uppercasee', () => {
  it('create an instance', () => {
    let pipe = new UppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
